module.exports = {
  projectId: "D3qN3R",
  /**
   * We can just add the director service URL here if we decide to use a self hosted version of Sorry Cypress
   * cloudServiceUrl: "http://localhost:1234",
   */
  e2e: { batchSize: 1 },
};
